{
  "name": "orcs-want-axes",
  "version": "1.0.0",
  "description": "Orcs want axes, elves want potions! Craft the right items and keep your customers happy—or watch them storm off in a huff. Time's ticking, so don't mix up your orders!",
  "scripts": {
    "start": "sdk-commands start",
    "deploy": "sdk-commands deploy",
    "build": "sdk-commands build",
    "upgrade-sdk": "npm install --save-dev @dcl/sdk@latest",
    "upgrade-sdk:next": "npm install --save-dev @dcl/sdk@next"
  },
  "devDependencies": {
    "@dcl/js-runtime": "7.14.0",
    "@dcl/sdk": "7.14.0"
  },
  "engines": {
    "node": ">=16.0.0",
    "npm": ">=6.0.0"
  },
  "prettier": {
    "semi": false,
    "singleQuote": true,
    "printWidth": 120,
    "trailingComma": "none"
  }
}